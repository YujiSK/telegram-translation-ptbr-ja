# worker-configuration.d.ts is generated by `wrangler types` (npm run
# cf-typegen) and must never be hand-edited — see docs/project-rules.md.
# Exclude it from whitespace checks (git diff --check) since its
# trailing-whitespace formatting comes from Wrangler, not from us.
worker-configuration.d.ts -whitespace linguist-generated=true
